/*
This is needed to run this code outside of Skills network labs.
Instead of using the lab environment's free access to OpenAI API,
we need to provide our own API key. Set the key here, and also
make make the corresponding changes in openai.js. 
*/
module.exports = {
    OpenAIAPIKey: 'YOUR_API_KEY'
};
